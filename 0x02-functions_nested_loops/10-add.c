#include <stdio.h>
#include "main.h"

/**
  * add - adds two integers and returns the result
  * @a: added to b
  * @b: added to a
  * Return: sum of a and b
*/
int add(int a, int b);
{
	int a = 0;
	int b = 0;

	a + b = c;
	return (c);

}
