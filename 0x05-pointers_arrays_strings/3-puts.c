#include "main.h"
#include <stdio.h>

/**
 * _puts - prints out a string.
 * @str: string
 *
 * Return: Always 0.
 */
_puts(char *str)
{
		char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);

	return (0);
}
